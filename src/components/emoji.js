const Emoji = () => {
  const emoji = Array(10 * 50).fill("🌲");
  return emoji;
};

//   function Emoji() {
//     const emoji = ['🌲'];

//     for (let i = 0; i < emoji.length; i++) {
//       return emoji;
//     }
// }

export default Emoji;
